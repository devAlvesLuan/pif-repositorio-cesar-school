#include <stdio.h> /* biblioteca para entrada e saída de dados  */

int main(void) {
    
    /* definindo uma váriavel float como "euler" e atribuindo o valor */
    float euler = 2.718281828459045;

    /* utilizando o printf para exibir o valor da variável
     * usando o %.3f para mostrar apenas 3 casas decimais do valor da várivel
     */
    printf("O valor de Euler é: %.3f\n", euler);
    return 0;
}